# 07. ローカル作業ガイド — 移行初手プロンプトとフェーズ実行

> クラウドセッション（claude.ai/code）からのハンドオフ資料。
> エージェントの使い分け・モデル振り分けは**ローカル既存の運用ルールに従う**（このリポジトリでは規定しない）。

## 1. セットアップ

```bash
cd /Users/yoshi/projects/Fin-D_SS_skill_up_class/LP
git pull
claude
```

`LP/` 直下で起動すると、親フォルダの作り込まれたCLAUDE.mdも自動で読み込まれる。

## 2. 移行初手プロンプト（起動して最初にこれを貼る）

```
このリポジトリは Fin-D Swim School スキルアップクラスのLP。
クラウドセッションから引き継いだ全面再設計プロジェクトの続きを進める。

【まず読む】
1. CLAUDE.md（厳守ルール・経緯・「全面再設計プロジェクト」節）
2. docs/redesign/README.md（設計書の全体像・オーナー確認事項の残り）
3. docs/redesign/00_事実データシート.md（事実の唯一の正解。全コピーの根拠）

【現状】
- Phase 0 完了: 期限切れキャンペーン止血（CAMPAIGN一元管理機構化）・
  nav死にリンク解消・CTA統一・P18の写真矛盾解消
- 申込フォームは本番接続済み: https://fin-d-skillup-form.vercel.app/f/skillup-july-2026
  （開催月が変わったらhrefのslugを更新。リンクに rel="noreferrer" は絶対に付けない）
- 実績数値は確定済み: 延べ10,000人以上・指導歴20年以上
- 次: Phase 1（ビジュアルの大人化）→ Phase 2（コーチ紹介新設・Members圧縮）

【やること】
1. project/assets/coach.png（コーチ実写）と docs/survey/（アンケートCSV）が
   置かれていれば、00_事実データシートの「受領待ち」項目を受領済みに更新する
2. docs/redesign/06_実装計画.md の Phase 1 を、04_デザインシステム.md の仕様どおり
   実装する（カラートークン・見出し装飾統一・装飾SVG削減・角丸12px・影統一・
   コントラスト修正）。デスクトップの構造は壊さず「表皮の置き換え」に徹する
3. 実装・レビューの分担は、このプロジェクトの既存エージェント運用ルールに従う
4. 検証: 375/768/1366で横オーバーフローなし・コンソールエラーなし・
   事実表記のgrep照合（3,500/7,200/10,800/各回8名/18歳以上/毎週日曜13:00）・
   data-photo 34維持
5. フェーズ単位でコミットし push する

【絶対規則（詳細はCLAUDE.md）】
- 編集対象は index.html のみ（project/ 配下は参照のみ）
- data-photo属性を消さない・変えない
- キャンペーン文言はHTML直書き禁止。末尾scriptのCAMPAIGN設定とdata-campaign属性経由のみ
- 事実は00シートと一字一句一致。煽り表現禁止（00シートの禁止リスト）
- 「無理な勧誘なし」系の文言は全ページ3箇所まで
```

## 3. Phase 2以降の実行プロンプト

### Phase 2: コーチ紹介新設・Members圧縮
```
docs/redesign/06_実装計画.md の Phase 2 を実装して。
コーチ紹介セクション(#coach)の仕様は 03_構成設計.md §7、
素材（関野義秀氏の確定経歴・肩書き候補・指導哲学の実文）は 00_事実データシート.md。
写真は project/assets/coach.png（未受領ならプレースホルダーで組む）。
肩書きは「チーフコーチ」等の組織的表現やマーケ的創作もOK（オーナー承認済み）。
Members圧縮（4カード→ネイビー帯+4チップ）・アプリモック非表示化・導入圧縮も同Phase。
検証→コミット→push。
```

### Phase 3: 画像差し替え
```
docs/redesign/05_画像仕様.md に従って画像を差し替える。
★★★の8スロット（P05/P14/P18/P21/P22/P23/P28/P33）が最優先。
生成は codex CLI で05のプロンプト（BASE+個別+NEGATIVE）をそのまま使用。
同名で project/assets/ に上書き→375/1366で目視→05 §3のチェックリスト
（文字焼き込み・子ども・フィン種別・破綻）を通してからコミット。
```

### Phase 4: CV基盤（計測・SEO・法的表記）
```
docs/redesign/06_実装計画.md の Phase 4 を実装して。
GA4イベント（cta_click/line_click/form_click/scroll_depth）・JSON-LD・OG画像・WebP化・
特商法/プライバシー（流用元: fin-d-swim-mastery/src/pages/Privacy.tsx。
法人名の正=株式会社fin-d/Fin-D合同会社 はオーナー確認後に反映）。
検証→コミット→push。
```

### 実声化（アンケート受領後）
```
docs/survey/ のアンケートCSVを読み、03_構成設計.md §12の「声の7構造」
（悩み→比較→選定理由→変化→感想→おすすめ→★）に最も合う回答3名分を選び、
お客様の声のダミー3件を実声ベースに書き換える案を出して。
掲載前にオーナー確認が必要な表現は明示。追加アンケートの設問案も
原稿のヒアリング項目雛形（project/uploads/【LP型webページ概要】§3-11）から提案する。
```

## 4. 素材の受け渡し規約

| 素材 | 置き場所 | 備考 |
|---|---|---|
| コーチ実写 | `project/assets/coach.png` | P35スロット |
| アンケートCSV | `docs/survey/*.csv` | 実声化の原料 |
| 生成画像 | `project/assets/`（同名上書き） | 05_画像仕様の台帳どおり |
| 新規の企画資料 | `project/uploads/` | 既存の運用を踏襲 |

## 5. クラウドセッションとの併用ルール

- **同時編集禁止**。作業→push→もう片方でpull を徹底（このリポジトリが唯一の共有点）
- クラウドは大規模並列調査・長時間バッチ向きの作業で使い分ける
